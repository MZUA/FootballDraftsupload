<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="4dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackgroundBorderless"> <!-- Ripple effect -->

    <ImageView
        android:id="@+id/imageViewTshirtBackground"
        android:layout_width="70dp"
        android:layout_height="80dp"
        android:src="@drawable/tshirt_icon_empty"
        android:scaleType="fitCenter"
        android:contentDescription="T-shirt icon"/>

    <TextView
        android:id="@+id/textViewPlayerInfo"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        tools:text="ST"
        android:textColor="@android:color/white"
        android:textSize="12sp"
        android:textStyle="bold"
        android:paddingBottom="10dp"/> <!-- Adjust padding to position text on shirt -->
</FrameLayout>